server:
  port: 8081  # Puerto para el servidor de Spring Boot Admin

spring:
  application:
    name: admin-server

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
